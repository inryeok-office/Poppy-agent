# Commit convention

Use Conventional Commits:

```text
type(scope): subject
```

Examples:

- `chore(init): agent development environment`
- `feat(agent): lifecycle coordination`
- `fix(server): heartbeat retry handling`

Use a concise English subject focused on the change. Avoid vague subjects such as
`update`, `work`, or `fix`, and do not add a trailing period.

